<nav class="z-10">
	<div class="content flex h-full">
		<a href="/" id="title" class="flex gap-3 items-center">
			<img src="/icons/hcb-light.png" alt="HCB logo" class="w-8 h-8" />
			 HCB
		</a>

		<div id="links" class="flex">
			<a href="/why-hack">Why Hack?</a>
			<a href="what-to-know">What to know</a>
			<a href="https://hackclub.com/hcb/apply" id="apply">Apply</a>
		</div>
	</div>
</nav>
<div id="pushdown" aria-hidden="true"></div>

<style>
	a {
		text-decoration: none;
		color: var(--white);
	}

	nav {
		position: fixed;
		height: var(--nav-height);
		left: 0;
		right: 0;
		top: 0;

		background-color: var(--dark-green);
	}

	#links {
		gap: 3rem;
		font-family: var(--font-sans);
	}

	#title {
		font-size: 1.5rem;
		font-family: var(--font-serif);
	}

	a#apply {
		text-transform: uppercase;
		font-weight: bold;
	}

	.content {
		height: 100%;
		display: flex;
		align-items: center;
		justify-content: space-between;
	}

	#pushdown {
		height: var(--nav-height);
	}
</style>
